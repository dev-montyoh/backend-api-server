spring:
  profiles:
    active: local # default profile
    group:
      local:
        - common
      test:
        - common
      prod:
        - common

---
# 공통
spring:
  config:
    activate:
      on-profile: common
  flyway:
    enabled: false
server:
  port: 3003

springdoc:
  swagger-ui:
    path: /docs